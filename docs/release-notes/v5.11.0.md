# Release Note for v5.11.0

- GuardLoop × SkillFoundry integration: self-improving AI governance hooks + `/guardloop` skill.
- `failure-scan.sh` (PostToolUse): real-time CRITICAL pattern detection on every file write.
- `guardloop-harvest.sh` (Stop): full 10-pattern session harvest logged to `memory_bank/`.
- `agents/_guardloop-rules.md`: adaptive rules file that self-populates from promoted patterns.
- `scripts/guardloop-analyze.sh` and `scripts/guardloop-promote.sh`: skill backend scripts.
- Version bump across all package files, MCP server, and site.
